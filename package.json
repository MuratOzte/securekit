{
  "name": "securekit-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run --workspaces build",
    "dev": "echo \"Use: npm run -w @securekit/node-auth dev  |  npm run -w demo-web dev\"",
    "lint": "echo \"(todo)\"",
    "typecheck": "echo \"(todo)\""
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "eslint": "^9.12.0",
    "@typescript-eslint/parser": "^8.7.0",
    "@typescript-eslint/eslint-plugin": "^8.7.0",
    "prettier": "^3.3.3",
    "tsup": "^8.2.4",
    "tsx": "^4.19.1"
  }
}
